import { Component, OnInit } from '@angular/core';
import { Router } from '@angular/router';
@Component({
  selector: 'app-lsnpass',
  templateUrl: './lsnpass.page.html',
  styleUrls: ['./lsnpass.page.scss'],
})
export class LsnpassPage  {
  constructor(private route: Router) { }

 
}

